#!/usr/bin/env bash

# Due to a bug in PackageMaker scripts cannot be removed. When you have added
# a postinstall script, removing it is not an option. So we replace it with
# this stupid empty thing now -_-
